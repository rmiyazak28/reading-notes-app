# memoLake RT-02　リグレッションテストシナリオ

本ドキュメントは、searchMemos のDB側全文検索移行（GIN/pg_trgm活用）に伴い、再実施・追加実施が必要なマニュアルテストシナリオをまとめたものです。

---

## 1. SCR-06 全読書メモ検索画面 - 再実施テスト

### メタ情報

| 項目 | 内容 |
|---|---|
| 対象画面 | SCR-06 全読書メモ検索画面（/memos） |
| 種別 | 再実施（searchMemos のRPC化・DB側ページングへの統一） |
| 環境 | `http://localhost:3000`（開発環境） |
| ブラウザ | Chrome 最新版 |
| 結果凡例 | ✅ 合格 ／ ❌ 不合格 ／ — 未実施 |

### 事前準備

- [ ] `npm run dev` で開発サーバーが起動していること
- [ ] Supabase ダッシュボードで `search_memos` RPC関数が適用済みであること（マイグレーション `20260617123756_search_memos_rpc.sql` を実行済み）
- [ ] ログイン済みの状態であること
- [ ] テスト用書籍が複数件登録されていること（書籍名・著者名が異なる書籍を2件以上）
- [ ] テスト用メモが複数件登録されていること（お気に入りあり・なし 各1件以上）
- [ ] テスト用タグが付いたメモが1件以上登録されていること

---

### 1-1. テキスト検索のリグレッション（DB側RPC検索への切り替え確認）

実装方式がクライアント側フィルタからDB側 `ILIKE` 検索（RPC）に変わったため、同一の検索結果が返ることを確認する。

| # | 操作 | 期待結果 | 結果 | 備考 |
|---|---|---|---|---|
| 1-1-1 | 検索バーにメモ内容の一部を入力する | 約400ms後に入力文字を含むメモのみに絞り込まれる | — | 基本動作のリグレッション |
| 1-1-2 | 検索バーに書籍名の一部を入力する | その書籍のメモのみに絞り込まれる | — | |
| 1-1-3 | 検索バーに著者名の一部を入力する | その著者の書籍のメモのみに絞り込まれる | — | |
| 1-1-4 | 検索バーにタグ名の一部を入力する | そのタグが付いたメモのみに絞り込まれる | — | |
| 1-1-5 | 検索バーをクリアする | 全件表示に戻る | — | |
| 1-1-6 | ヒットしないキーワードで検索する | 「メモが見つかりません」が表示される | — | 空状態のリグレッション |
| 1-1-7 | URLをコピーして別タブで開く（q=パラメータ付き） | 同じ検索結果が表示される | — | |

---

### 1-2. お気に入りフィルタ・ソートのリグレッション

| # | 操作 | 期待結果 | 結果 | 備考 |
|---|---|---|---|---|
| 1-2-1 | 「お気に入りのみ」をONにする | favorite=trueのメモのみ表示される | — | |
| 1-2-2 | 「お気に入りのみ」をOFFにする | 全件表示に戻る | — | |
| 1-2-3 | ソートを「更新日順」に切り替える | 更新日の降順でメモが並び替わる | — | |
| 1-2-4 | ソートを「登録日順」に戻す | 登録日の降順でメモが並び替わる | — | |

---

### 1-3. テキスト検索とフィルタの組み合わせ確認（RPC引数伝達の確認）

query × favoriteOnly × sortBy の組み合わせがRPCの引数に正しく渡されることを、操作結果で確認する。

| # | 操作 | 期待結果 | 結果 | 備考 |
|---|---|---|---|---|
| 1-3-1 | テキスト検索後に「お気に入りのみ」をONにする | 検索キーワードに一致し、かつfavorite=trueのメモのみ表示される | — | AND絞り込みの確認 |
| 1-3-2 | テキスト検索後にソートを切り替える | 絞り込み結果を保ったまま並び順が変わる | — | |
| 1-3-3 | テキスト検索 + お気に入りON + ソート更新日順を同時に適用する | 3条件すべての絞り込みが正しく適用される | — | |

---

### 1-4. 「さらに読み込む」のキャッシュ方式撤廃確認（重点確認）

キャッシュ方式からDB側ページング（offset）に統一されたため、全パターンを再確認する。

| # | 操作 | 期待結果 | 結果 | 備考 |
|---|---|---|---|---|
| 1-4-1 | メモが50件以上ある状態で初期表示する | 「さらに読み込む」ボタンが表示される | — | |
| 1-4-2 | 「さらに読み込む」をクリックする（検索条件なし） | 次の50件が追記され、合計件数が増える | — | |
| 1-4-3 | 全件読み込んだ後を確認する（検索条件なし） | 「さらに読み込む」ボタンが非表示になる | — | |
| 1-4-4 | 50件超のメモがある状態でキーワード検索する | 「さらに読み込む」ボタンが表示される（検索結果が50件超の場合） | — | 旧実装はキャッシュ方式だったが、新実装はDBへ追加リクエスト |
| 1-4-5 | 検索結果に対して「さらに読み込む」をクリックする | 同じ検索条件でDBへ追加取得され、次の50件が追記される | — | キャッシュ方式撤廃後の動作確認（重点） |
| 1-4-6 | 全件読み込んだ後を確認する（検索条件あり） | 「さらに読み込む」ボタンが非表示になる | — | |
| 1-4-7 | 検索条件を変更した後を確認する | 先頭50件にリセットされ、「さらに読み込む」ボタンが再表示される（該当件数が50件超の場合） | — | SCR-06.md 11-4（過去不合格→修正済み）のリグレッション |

---

### 1-5. 1000件超のメモが登録されたユーザーでの検索漏れ確認

旧実装では上限1000件のクライアント側フィルタだったため、1000件超のユーザーで検索漏れが発生していた。RPC化により解消されていることを確認する。

| # | 操作 | 期待結果 | 結果 | 備考 |
|---|---|---|---|---|
| 1-5-1 | 1000件超のメモがある状態でキーワード検索する | 1000件目以降のメモもヒットし、検索漏れが発生しない | — | 再現困難な場合は備考に「再現困難」と記載。DBに直接多数データを投入して確認するか、代替としてE2Eの「さらに読み込む」テスト（51件）で原理確認とする |

---

## 不合格時の記録欄

不合格（❌）となったケースは以下に記録する。

| # | 画面/モーダル | 内容 | 対応状況 |
|---|---|---|---|
| | | | |
